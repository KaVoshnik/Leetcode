public class Solution {
    public int ClimbStairs(int n) 
    {
        if (n <= 2) return n > 0 ? n : 1;
        
        int a = 1, b = 1;
        for (int i = 2; i <= n; i++)
        {
            int temp = b;
            b = a + b;
            a = temp;
        }
        return b;
    }
}
